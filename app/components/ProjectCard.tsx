import type { Project } from "../data/site";
export function ProjectCard({project,index}:{project:Project;priority?:boolean;index:number}) { return <article className={`project-card project-card--${index+1}`}><div className="project-card__topline"><span>0{index+1}</span><span>{project.eyebrow}</span></div><div className="project-card__identity"><h3>{project.name}</h3></div><p className="project-card__description">{project.contribution}</p><p className="project-card__tags">{project.skills.join(" · ")}</p></article>; }
