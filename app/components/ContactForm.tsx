const endpoint=process.env.NEXT_PUBLIC_FORMSPREE_ENDPOINT?.trim();
const enabled=Boolean(endpoint);
export function ContactForm(){return <form className="contact-form" action={enabled?endpoint:undefined} method="POST" data-disabled={!enabled}><div className="form-field"><label htmlFor="name">Name</label><input id="name" name="name" autoComplete="name" required disabled={!enabled}/></div><div className="form-field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" autoComplete="email" required disabled={!enabled}/></div><div className="form-field form-field--wide"><label htmlFor="message">Message</label><textarea id="message" name="message" rows={5} required disabled={!enabled}/></div><div className="form-actions form-field--wide"><button type="submit" disabled={!enabled}>{enabled?"Send message":"Contact form unavailable"}</button>{!enabled&&<p role="status">Set NEXT_PUBLIC_FORMSPREE_ENDPOINT to enable this form.</p>}</div></form>}
